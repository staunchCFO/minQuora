include includes/header.pug

block content
  div.container
    h1.pt-4 #{title}
    form(method='POST')
      #form-group
        label Title:
        input(name='title' , type='text', value=article.title disabled="disabled")
      #form-group
        label Author:
        -let userID = `${user.firstName} ${user.lastName}`
        input(name='author' , type='text', value=userID disabled="disabled")
      #form-group
        label Body:
        textarea(name='body' , type='text')=article.body
      a.mt-4.btn.btn-primary.text-white(type='submit' , value='Edit') Update
      div(class="float-right")
        -let deleteUrl = "/articles/delete" + article.single
        a.btn.btn-danger.delete-article.mt-4(href=deleteUrl , data-id=article._id) Delete
        
